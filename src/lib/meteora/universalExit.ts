export function plan() {
  return { steps: [], notes: ['stub'], estUnits: 0, skips: [] };
}

export function execute() {
  return { status: 'stub', signatures: [] };
}

// Universal exit helpers (stubs)
export function universalExit() {
  return null;
}
