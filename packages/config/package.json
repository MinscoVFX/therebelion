{
  "name": "@meteora-invent/config",
  "description": "Shared configuration for Meteora Invent monorepo",
  "version": "0.0.0",
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules .turbo"
  }
}
