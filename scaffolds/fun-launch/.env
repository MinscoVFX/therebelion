# ---------------- Cloudflare R2 Storage ----------------
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_ACCOUNT_ID=your_r2_account_id
R2_BUCKET=your_r2_bucket_name

# ---------------- Solana RPC ----------------
# Normal Solana RPC (Helius API key here)
RPC_URL=https://rpc.helius.xyz/?api-key=YOUR_KEY

# Jito / Helius Sender endpoint for bundle submission
JITO_RELAY_URL=https://sender.helius-rpc.com/fast

# ---------------- Meteora Pool Config ----------------
POOL_CONFIG_KEY=your_pool_config_key

# ---------------- Public URLs ----------------
R2_PUBLIC_BASE=your_r2_public_base
PUBLIC_BUCKET_URL=your_site_url

# ---------------- Fees ----------------
# Creation fee receiver (public wallet OK)
NEXT_PUBLIC_CREATION_FEE_RECEIVER=YOUR_PARTNER_WALLET_BASE58

# Default site for "created on" field
DEFAULT_CREATEDON=your_site_url

# ---------------- Raydium LaunchLab (Platform Config) ----------------
# Platform PDA created once via the setup script (we'll add it later)
RAYDIUM_PLATFORM_PDA=
# Authority that administers the platform (JSON array or base58 secret key)
RAYDIUM_PLATFORM_AUTHORITY_SECRET=

# Your platform cut on the bonding curve (in basis points)
RAYDIUM_SHARE_FEE_BPS=20

# One-time creation fee you charge (same pattern as DBC)
RAYDIUM_CREATION_FEE_LAMPORTS=25000000

# Default locked-LP split on migration (sum should be 10000 bps = 100%)
RAYDIUM_MIGRATE_SPLIT_PLATFORM_BPS=0
RAYDIUM_MIGRATE_SPLIT_CREATOR_BPS=1000
RAYDIUM_MIGRATE_SPLIT_BURN_BPS=9000
