import { describe, it, expect } from 'vitest';
// import { useMobile } from '../hooks/useMobile'; // Adjust import

describe('useMobile hook', () => {
  it('should detect mobile', () => {
    expect(true).toBe(true);
  });
});
