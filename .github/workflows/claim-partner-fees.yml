name: Claim Partner Fees

on:
  workflow_dispatch:

jobs:
  claim-partner-fees:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        run: npm install -g pnpm@10

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Decode PK_B58 to keypair.json
        run: |
          npx -y -p bs58@4 node -e "
            const fs = require('fs');
            const bs58 = require('bs58');
            const decode = (bs58.default || bs58).decode;
            const s = process.env.PK_B58?.trim();
            if (!s) throw new Error('Missing PRIVATE_KEY_B58');
            const arr = Array.from(decode(s));
            fs.writeFileSync('keypair.json', JSON.stringify(arr));
          "
        env:
          PK_B58: ${{ secrets.PK_B58 }}

      - name: Run claim trading fee script
        run: npx -y -p tsx tsx studio/src/scripts/dbc/claim_trading_fee.ts

      - name: Clean up keypair.json
        run: rm -f keypair.json
