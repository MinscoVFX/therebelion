### Core RPC configuration
RPC_ENDPOINT="https://YOUR_MAINNET_RPC" # Primary server-side RPC (preferred)
RPC_URL="https://YOUR_MAINNET_RPC"      # Optional legacy alias (fallback chain)
NEXT_PUBLIC_RPC_URL="https://YOUR_MAINNET_RPC" # Exposed to client when needed

### DBC Program + Discriminators (MANDATORY)
# Real DBC program id (override default only if different)
DBC_PROGRAM_ID="dbcij3LWUppWqq96dh6gJWwBifmcGfLSB5D4DuSMaqN"

# EXACT 8-byte (16 hex chars) discriminator for fee claim instruction.
# WARNING: The value below is a PLACEHOLDER. DO NOT USE ON MAINNET.
// Replace with the real 16-hex discriminator or leave blank and set an instruction name.
DBC_CLAIM_FEE_DISCRIMINATOR="ffffffffffffffff" # <-- REPLACE BEFORE MAINNET
# Anchor instruction name (if you prefer name-based derivation):
# DBC_CLAIM_FEE_INSTRUCTION_NAME="claim_partner_trading_fee"

# Withdraw discriminator (liquidity removal). Feature NOT ACTIVE yet.
# Leave unset until the withdraw path is implemented. Placeholder below for documentation only.
DBC_WITHDRAW_DISCRIMINATOR="eeeeeeeeeeeeeeee" # INACTIVE / PLACEHOLDER
# DBC_WITHDRAW_INSTRUCTION_NAME="withdraw_liquidity" # INACTIVE

# If you maintain an Anchor-style IDL at repo root (dbc_idl.json) set this to true to auto-derive discriminators:
# DBC_USE_IDL=true

### Safety / Allow Lists
# Optional: restrict allowed DBC program IDs (comma separated) for defense in depth
# ALLOWED_DBC_PROGRAM_IDS="dbcij3LWUppWqq96dh6gJWwBifmcGfLSB5D4DuSMaqN"

### Priority / Performance (optional tuning defaults handled in code)
# DEFAULT_PRIORITY_MICROS=250000
# DEFAULT_COMPUTE_UNIT_LIMIT=800000

### Misc existing environment keys referenced elsewhere
# POOL_CONFIG_KEY="<base58>"

### Do NOT set this in production (only for local experimentation). Disables guards.
# ALLOW_PLACEHOLDER_DBC=true

### Logging noise suppression (not needed now that placeholders removed)
# DBC_SUPPRESS_PLACEHOLDER_WARNING=true
#############################################
# therebelion Monorepo Environment Example  #
#############################################
# Copy to .env.local (Next.js dev) and/or .env for scripts. Never commit secrets.

############################
# RPC / Network
############################
# Public RPC endpoint (used client-side). Can be left blank to use default mainnet.
RPC_URL=""
# Prefer NAME path; we’ll auto-detect role at runtime:
DBC_CLAIM_FEE_INSTRUCTION_NAME="auto"            # "auto" means: try creator, fall back to partner
# Optional if you insist on discriminator (else omit):
# DBC_CLAIM_FEE_DISCRIMINATOR=""
ALLOWED_DBC_PROGRAM_IDS='["dbcij3LWUppWqq96dh6gJWwBifmcGfLSB5D4DuSMaqN"]'
ALLOWED_DAMM_V2_PROGRAM_IDS='["cpamdpZCGKUy5JxQXB4dcpGPiikHawvSWAd6mEn1sGG"]'
# Optional while env-driven filters exist:
# MIGRATED_DBC_POOLS='["<poolPubkey1>","<poolPubkey2>"]'
# (Removed accidental duplicate ALLOWED_DBC_PROGRAM_IDS line)

############################
# Priority & Performance (optional tunables)
############################
# Default microLamports per compute unit for exits if UI value absent.
DEFAULT_PRIORITY_MICROS=250000
# Optional compute unit limit for fast mode (empty = none)
DEFAULT_COMPUTE_UNIT_LIMIT=

############################
# Misc / Diagnostics
############################
LOG_LEVEL=info
COMMITMENT=confirmed

# End of file
