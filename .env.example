#############################################
# therebelion Monorepo Environment Example  #
#############################################
# Copy to .env.local (Next.js dev) and/or .env for scripts. Never commit secrets.

############################
# RPC / Network
############################
# Public RPC endpoint (used client-side). Can be left blank to use default mainnet.
NEXT_PUBLIC_RPC_URL=https://api.mainnet-beta.solana.com
# Private / higher‑quota RPC for server routes or studio scripts (optional)
RPC_URL=

############################
# DBC Program Configuration
############################
# DBC dynamic bonding curve program ID (Solana public key). If unset, fallback default is used.
DBC_PROGRAM_ID=
# 8-byte instruction discriminator (first 16 hex chars) for the claim fees instruction (little-endian).
# Replace the placeholder below with the real value before production.
DBC_CLAIM_FEE_DISCRIMINATOR=0102030405060708
# (Future) Withdraw liquidity discriminator once spec is finalized.
DBC_WITHDRAW_LIQUIDITY_DISCRIMINATOR=
# Use local dbc_idl.json to auto-derive discriminators (true/false)
DBC_USE_IDL=false
# Allow placeholder discriminator even in production (NOT recommended).
ALLOW_PLACEHOLDER_DBC=false
# Suppress console warning about placeholder discriminator (dev convenience only)
DBC_SUPPRESS_PLACEHOLDER_WARNING=false
# Comma-separated allow list of permitted DBC program IDs (optional safety gate)
ALLOWED_DBC_PROGRAM_IDS=

############################
# Priority & Performance (optional tunables)
############################
# Default microLamports per compute unit for exits if UI value absent.
DEFAULT_PRIORITY_MICROS=250000
# Optional compute unit limit for fast mode (empty = none)
DEFAULT_COMPUTE_UNIT_LIMIT=

############################
# Misc / Diagnostics
############################
LOG_LEVEL=info
COMMITMENT=confirmed

# End of file
