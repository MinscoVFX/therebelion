# Environment configuration for Meteora DBC integration
# Copy this file to .env.local (local dev) or provide these as deployment secrets.
# Replace the placeholder values with real program + discriminator.

# DBC dynamic bonding curve program ID (Solana public key)
DBC_PROGRAM_ID=<REAL_DBC_PROGRAM_PUBKEY>

# 8-byte instruction discriminator (first 16 hex chars) for the claim fees instruction.
# DO NOT leave the placeholder or the builder will throw in production.
DBC_CLAIM_FEE_DISCRIMINATOR=<REAL_16_HEX_CHARS>

# Set to "true" ONLY if you want to bypass the production placeholder guard (NOT recommended)
ALLOW_PLACEHOLDER_DBC=FALSE
